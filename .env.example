# Dit bestand wordt door docker-compose.yml gebruikt.
# Kopieer naar .env en vul een eigen JWT_SECRET in
# (genereer er een met: openssl rand -hex 32)
JWT_SECRET=verander_dit_naar_een_lange_random_string
